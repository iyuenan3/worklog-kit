# AGENTS.md

The single source of truth for this repo's conventions is [CLAUDE.md](CLAUDE.md) (router) plus `AIREADME/`. Any agent (Codex etc.) working in this repo follows the same contract, especially the three hard rules: `diaries/` is append-only, do not delete `wiki/` section anchors, credentials never enter git.
