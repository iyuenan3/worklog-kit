# DECISIONS (append-only)

> Each entry = number + date + `Problem / Constraint / Decision / Alternatives(rejected) / Tradeoff`. Schema evolution (wiki structure, diary sections, config semantics) gets its rationale recorded here, so that you-in-six-months understands why.

(empty: the first entry usually appears the first time you want to change the wiki structure)
