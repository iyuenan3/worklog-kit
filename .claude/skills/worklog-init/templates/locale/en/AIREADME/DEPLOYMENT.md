# DEPLOYMENT

N/A: local execution + a private GitHub repo is the entire deployment. The single ops constraint: repository visibility must stay **private** (ingest checks automatically before every push and aborts with a warning on public).
