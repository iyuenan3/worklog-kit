# CHANGELOG (append-only)

> Reverse-chronological version blocks, each = version + date + `Added/Changed/Fixed/Removed`. Vault milestones (initialization, major schema changes, source additions/removals) are recorded here.

(empty: the first entry = your initialization date)
