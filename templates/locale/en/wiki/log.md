# Operations Log

> Append-only: each ingest run prepends a `## [YYYY-MM-DD] ingest` section below the anchor. Rotate to `log-YYYY.md` yearly to avoid large-file read truncation.

<!-- ingest:log-anchor (new entries go below this line; do not delete) -->
