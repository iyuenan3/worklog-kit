# RELATIONS

## 出向依赖
- **worklog-kit**（upstream，https://github.com/iyuenan3/worklog-kit）：skill 更新来源；worklog-update 只同步 `.claude/skills/`，永不触碰本 vault 数据
- **我的各子项目**：经 `worklog.config.yaml` 的 `sources` 扫描；各自真相源在其自身 `AIREADME/`（若有）

## 入向（谁用我）
-（无：本 vault 不被其他项目依赖；若将来有消费方，如个人主页构建器读 `wiki/projects/`，在 SPEC 定契约并回填此处）

## 共享底座
-（无）
